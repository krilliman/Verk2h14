@model List<BookCave.Models.ViewModels.BookViewModel>
    <div>
        @foreach(var book in @Model)
        {
            <p>@book.Id , @book.Name , @book.Rating.ToString("0.0")</p>
        }
    </div>